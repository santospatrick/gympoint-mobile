import React from 'react';
import { View, Text } from 'react-native';

const Signin = () => {
    return (
        <View>
            <Text>Signin</Text>
        </View>
    );
};

export default Signin;
